# Review questions

1. Referenced condition `OK_SHUTOFF` has no definition in spec tables
2. Referenced condition `NOK_SHUTOFF` has no definition in spec tables
3. Referenced condition `FORCE_SHUTOFF` has no definition in spec tables
4. Referenced condition `CND_NORMAL_ROUTE` has no definition in spec tables
5. Referenced condition `CND_BACKUP_ROUTE` has no definition in spec tables
6. Referenced condition `CND_BACKUP_TIMER_OK` has no definition in spec tables
7. Referenced condition `POWER=OFF` has no definition in spec tables
8. Referenced condition `REQ_MAIN_OK (*1)` has no definition in spec tables
9. Referenced condition `REQ_STABLE (*4)` has no definition in spec tables
10. Referenced condition `REQ_SRC_A_VALID (*2)` has no definition in spec tables
11. Referenced condition `REQ_SRC_B_VALID (*3)` has no definition in spec tables
12. Referenced condition `VEHICLE_STOPPED (*1)` has no definition in spec tables
13. Referenced condition `DRIVER_SAFE (*2)` has no definition in spec tables
14. Referenced condition `SAFETY_LOCKED (*5)` has no definition in spec tables
15. Referenced condition `PROCESS_IDLE (*3)` has no definition in spec tables
16. Referenced condition `PROCESS_PREPARED (*4)` has no definition in spec tables
